# Local Ollama backend — no API key needed. Just have Ollama running.
OLLAMA_URL=http://127.0.0.1:11434

# Model to use. qwen2.5:7b = best quality here; qwen2.5:3b = faster on CPU.
OLLAMA_MODEL=qwen2.5:7b

# Proxy port (must match the proxy target in vite.config.js)
PORT=8787
